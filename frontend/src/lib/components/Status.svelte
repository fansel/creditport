<!-- <script>
  import { getStatus } from '$lib/config';
  export let status;
  export let extern = false;
</script>

<div class="ms-2 badge rounded-pill d-inline-flex align-items-center" style="background-color: {getStatus(status).color}; color: {getStatus(status).text_color};">
  {#if extern}
    {getStatus(status).text_extern}
  {:else}
    {getStatus(status).text_intern}
  {/if}
</div> -->
